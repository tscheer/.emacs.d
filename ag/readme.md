This directory contains a global ignore file for the silver searcher tool.

The file needs to be installed into: `$HOME/.agignore`

It just contains a list of patterns for the tool to ignore when performing a
search.

